# dask_task_app

## Quick Start

```bash
python -m venv .venv
source .venv/bin/activate
pip install -e .[dev]
python -m dask_task_app.main
```

## Pre-commit

```bash
pre-commit install
pre-commit run --all-files
```
